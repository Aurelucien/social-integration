# 协作指南

感谢参与这个项目。这里采用轻量协作流程：让改动容易理解、容易验证，并始终保护真实
个人消息和本机状态。

## 开始之前

1. 使用 Python 3.11 或更高版本。
2. 在 `personal-social-inbox/` 内创建自己的 `.venv`；该目录不会进入 Git。
3. 不要复制或索取仓库所有者的真实数据库、消息导出、附件目录、账号绑定或采集配置。
4. 需要测试数据时，优先扩展仓库中的虚构、小型、可公开 fixture。

## 变更原则

- 保持源应用和源材料只读，写入仅限项目自己的输出目录。
- 保持导入与 MCP 查询分离；MCP 接口只提供有界读取。
- 保留来源、缺失状态和不确定性，不用模型推断覆盖源记录。
- 新增平台或客户端版本时，明确写出已验证版本、输入格式和未覆盖边界。
- 不提交凭据、令牌、真实账号、聊天标识、机器绝对路径、私有附件或运行数据库。
- 不在普通功能改动中顺带加入客户端注入、自动登录、解密、发消息或后台常驻服务。

## 开发与验证

主项目的基本检查是：

```bash
cd personal-social-inbox
python3 -m unittest discover -s tests -v
python3 -m compileall -q src tests social_inbox.py server.py
```

如果改动了某个实验原型，还应运行对应目录中的测试。例如：

```bash
python3 -m unittest discover -s experiments/wechat-4.1.7-poc -p 'test_*.py' -v
python3 -m unittest discover -s experiments/dingtalk-8.3.5-poc -p 'test_*.py' -v
```

提交前查看完整差异和未跟踪文件，并确认测试输出没有泄露本机路径或个人数据。

## 提交和评审

- 每个提交处理一个连贯问题，标题说明实际行为变化。
- 在合并说明中写明：改了什么、为什么、运行了哪些检查、哪些检查没有运行。
- 涉及数据格式、隐私边界、工具契约或支持版本的改动，应同步更新相邻文档和测试。
- 评审重点是行为和证据；通过测试不代表已经在真实账号、已安装插件或所有客户端版本上
  验证。

## 公开发布

本仓库目前没有开源许可证，也没有正式发布承诺。若要从受信任协作转为公开发布，请先
单独完成许可证选择、第三方归属审查、敏感信息复核和支持范围确认；这些不是普通合并
请求可以默认决定的事项。
